<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <title>title</title>
  </head>
  <body>
    <h1>Today</h1>
    <% date = (Date.today.strftime("%A, %B %d, %Y") %>

    <p><%= "The date is " + "#{date}" %>

  </body>
</html>
